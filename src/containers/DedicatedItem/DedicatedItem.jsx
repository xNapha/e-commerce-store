import React from "react";
import { useParams } from "react-router-dom";

const DedicatedItem = ({ id, title, price, description, image }) => {
    /*
    show name
    image
    description
    price
    form
    input for quantity
    button to add to cart


    */
    return <div>{title}</div>;
};

export default DedicatedItem;
